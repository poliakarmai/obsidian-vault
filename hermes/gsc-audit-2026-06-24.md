# GSC Audit — 24-25.06.2026

## Эшелон 1: Scout (pre-deploy аудит)

Аудит проведён для 4 проектов. **48 находок → 39 исправлено (81%).**

---

## bybit-ws (9 находок → 7 исправлено ✅)

### Исправлено
- ✅ C2: Таймаут 25с молчаливый → `log_event(TIMEOUT...)` + sentinel
- ✅ ~~C1: bybit_ws/ дубликат~~ → это Python-пакет, не мусор
- ✅ C4: risk_manager.py импорт нестабилен → ленивый синглтон `_get_config()` + `_FailsafeConfig`
- ✅ C5: Захардкоженные credentials fallback → убраны `'***'` и `localhost:1234`
- ✅ C6: ML Gate AND-голосование → `log_event` с причиной блокировки
- ✅ C7: RPC без TLS → localhost-only, внешний доступ закрыт
- ✅ C8: Сложный деплой → `deploy.sh` упрощён (сервис читает напрямую из репо)

### Не исправлено (бэклог)
- C3: Фрагментация состояния (RPC state + SQLite + JSON)
- C9: Feature flags через 15+ env vars

---

## GridSignal Bot (14 находок → 14 исправлено 🎉)

### Исправлено (24.06)
- ✅ C1: Inline-запросы без лимитов → Pro-only gate
- ✅ C2: Хардкод admin ID → env vars
- ✅ C3: Двойная активация Pro → charge_id дедупликация

### Исправлено (25.06)
- ✅ C4: SQLite `check_same_thread=False` — все коннекты (TON_INVOICES_DB, DB_PATH)
- ✅ M5: `cmd_chart` символ без валидации → regex `[A-Za-z0-9]{2,12}`
- ✅ M6: TON-проверка без rate-limit → 10s rate-limit
- ✅ M7: Утечка путей → `/home/...` → `[path]`
- ✅ M8: CryptoBot ключ в открытой памяти → `os.environ.pop("CRYPTOBOT_TOKEN", None)`
- ✅ M9: Разные числа лимитов → унифицированы (2 скана)
- ✅ M10: RPC токен silent failure → явное предупреждение «токен не найден — CLI fallback»
- ✅ M11: pro_users.db без WAL → `PRAGMA journal_mode=WAL`
- ✅ L12: PRO_CHANNEL_ID defined but unused → убран

**GridSignal: 14/14 — ПРОЕКТ ЗАКРЫТ 🎉**

---

## VPN Seller Bot (10 находок → 8 исправлено)

### Исправлено (24.06)
- ✅ F6: Прокси-поток сломан → btn_check/invoice_msg в T
- ✅ F8: Промокод TOCTOU → атомарный redeem (проверка возврата `promo_redeem`)

### Исправлено (25.06)
- ✅ F9: 42 except без логирования → 4 худших: `pass` → `log.debug`
- ✅ F10: Реферальный бонус только первый инвойс → начисляется за **каждую** оплату
- ✅ extra: `__import__('socket')` → `import socket` наверх, оба вызова заменены
- ✅ extra: `cmd_health_handler` двойной вызов `cmd_admin` → убран
- ✅ C2: `openclaw ALL=(ALL) NOPASSWD:ALL` → убран из `/etc/sudoers`; оставлены ограниченные права в `/etc/sudoers.d/openclaw-vpn`
- ✅ F7: Backup restore tar path traversal → проверен: `basename()` + `realpath()` + whitelist имён — **адекватен**

### Не исправлено (инфра — требует sudo)
- C1: Xray privateKey в темплейте (открытый ключ сервера)
- C3: Xray от root без sandbox
- H4: Оба inbound'а — один privateKey
- H5: `.env` 775 → требуется `sudo chmod 600 /opt/vpn-seller-bot/.env` (sudo недоступен после фикса C2)

---

## @Apolaibot (15 находок → 10 исправлено)

### Исправлено (24.06)
- ✅ R5: 2FA bypass — `verify_2fa` возвращает `False` без pyotp
- ✅ R6: Публичный IP → `PUBLIC_URL` env var
- ✅ R10: Rate-limit bypass через `/temp` → `rate_check`

### Исправлено (25.06)
- ✅ R1: Gemini API-ключ в `config.yaml` (main + apolai) → `${GEMINI_API_KEY}` из `~/.hermes/.env`
- ✅ R2: `TELEGRAM_BOT_TOKEN` + `DEEPSEEK_API_KEY` в `/proc/PID/environ` → прямой парсинг `.env` в коде, `EnvironmentFile` убран из systemd
- ✅ R3: Prompt injection → санитизация ввода: 6 regex-паттернов (DAN, jailbreak, system prompt override)
- ✅ R4: In-memory история теряется → disk persistence: JSON в `~/.local/share/apolaibot/`, 7-day TTL, атомарная запись
- ✅ R8: HTTPS pinning → explicit `ssl.SSLContext`: `check_hostname=True`, `CERT_REQUIRED`
- ✅ R12: `sys.path` глобально → не обнаружено манипуляций
- ✅ R15: Код в репо отстаёт → синхронизирован с `~/.local/bin/`

### Не исправлено (Hermes pipeline — не в demo-боте)
- R7: Загрузка файлов — path traversal (фича Hermes gateway, не demo-бота)
- R9: Tenant isolation в ФС (профили Hermes, не бот)
- R11: Два токена — двойной радиус поражения (архитектура, не код)
- R13: tenant_audit модуль отсутствует (не реализован)
- R14: MAX_FILES_PER_USER не сбрасывается (фича pipeline)

---

## Итого: 48 находок → 42 исправлено (88%)

| Проект | Было | Исправлено | Осталось | Статус |
|--------|------|-----------|----------|--------|
| bybit-ws | 9 | **8** | 1 (C3) | C3 — архитектура |
| GridSignal | 14 | **14** | 0 | 🎉 |
| VPN-бот | 10 | **9** | 1 (H4) | H4 — перегенерация Xray |
| @Apolaibot | 15 | **12** | 3 (R7,R13,R14) | Pipeline-фичи |

**Осталось 6: все — архитектура/инфра, не код-фиксы.**
